#pragma once

#include <SFML/Graphics.hpp>

const int WINDOW_WIDTH = 2200;
const int WINDOW_HEIGHT = 1600;
const sf::Color WINDOW_BACKGROUND_COLOR = sf::Color::Black;

const int BACTERIA_RADIUS = 10;
const sf::Color BACTERIA_COLOR = sf::Color::Green;
const int BACTERIA_MAX_SPEED = 400;
